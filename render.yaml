services:
  - type: web
    name: human-development-index-prediction
    env: python
    rootDir: Flask
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9